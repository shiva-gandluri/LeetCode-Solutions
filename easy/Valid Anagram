class Solution:
    def isAnagram(self, s: str, t: str) -> bool:
        s_dict = dict(Counter(s))
        t_dict = dict(Counter(t))
        
        if(t_dict == s_dict):
            return True
        
        return False
